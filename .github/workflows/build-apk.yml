name: Android Debug APK

on:
  workflow_dispatch:
  push:
    tags: ['v*.*.*']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: stable
          cache: true

      - name: Dependencias
        working-directory: ./coop_control_app_reports
        run: flutter pub get

      - name: Construir APK (debug)
        working-directory: ./coop_control_app_reports
        run: flutter build apk --debug

      - name: Artefacto
        uses: actions/upload-artifact@v4
        with:
          name: app-debug.apk
          path: coop_control_app_reports/build/app/outputs/flutter-apk/app-debug.apk

      - name: Crear Release con APK
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: coop_control_app_reports/build/app/outputs/flutter-apk/app-debug.apk
